# data-pipeline-s3-postgres
A simple  ETL pipeline made on Apache AirFlow. It extracts data from an S3 bucket, made some transformation on the data and insert it into a relational database (postgresql).
